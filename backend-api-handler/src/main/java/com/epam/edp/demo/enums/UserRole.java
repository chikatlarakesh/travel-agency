package com.epam.edp.demo.enums;

/**
 * Application roles used for access control.
 * The {@code name()} of each constant is what gets persisted in the User document.
 */
public enum UserRole {
    CUSTOMER,
    TRAVEL_AGENT,
    ADMIN
}
