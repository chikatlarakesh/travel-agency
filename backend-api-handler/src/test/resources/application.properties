de.flapdoodle.mongodb.embedded.version=7.0.0
spring.data.mongodb.uri=mongodb://localhost/testdb

# Increase rate limit for integration tests to avoid 429 across test classes
security.rate-limit.max-attempts=100
security.rate-limit.window-minutes=1

# Logging
logging.level.root=WARN
logging.level.com.epam.edp.demo=INFO
