apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "report-app.fullname" . }}-test-connection"
  labels:
    {{- include "report-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "report-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
