apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-app.fullname" . }}
  labels:
    {{- include "report-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-app.selectorLabels" . | nindent 4 }}
